var config = {
        database: {
            host: 'ip',
            user: 'user',
            password: 'password',
            database : 'dbName'
        },
        certs: {
            privKey: 'path2privkey/privkey.pem',
            cert: 'path2cert/cert.pem'
        }
};

module.exports = config;

		